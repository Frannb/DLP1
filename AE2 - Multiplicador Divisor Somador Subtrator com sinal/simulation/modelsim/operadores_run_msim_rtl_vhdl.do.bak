transcript on
if {[file exists rtl_work]} {
	vdel -lib rtl_work -all
}
vlib rtl_work
vmap work rtl_work

vcom -93 -work work {/home/francin.b/DLP I (20-1)/AE2 - Multiplicador Divisor Somador Subtrator com sinal/operadores.vhd}

